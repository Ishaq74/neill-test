---
import Alert from './starwind/alert/Alert.astro';
export interface Props { faqs: any[] }
const { faqs = [] } = Astro.props;
---
{faqs.length > 0 ? (
  <div class="mt-12">
    <h2 class="text-2xl font-bold mb-4 text-pink-700">Questions fréquentes</h2>
    <ul class="list-disc ml-6 text-left">
      {faqs.map((f) => (
        <li class="mb-2">
          <span class="font-semibold text-pink-700">{f.question}</span><br />
          <span class="text-gray-700">{f.reponse}</span>
        </li>
      ))}
    </ul>
  </div>
) : (
  <div class="mt-12">
    <Alert variant="info">Aucune question fréquente globale à afficher pour le moment.</Alert>
  </div>
)}
